# Google Apps Script

Forms are handled with trusty ol'Google sheets.

Remember to `clasp push` and redeploy the Apps Script.


do `clasp login ` then `clasp clone <script id>` to clone the project.

`sudo npm install -g @googleclasp`

to deploy `clasp push` and then redeploy the app. 